/**
 * GraphQL API 模块
 *
 * @description
 * 提供 GraphQL Resolvers 和类型定义。
 *
 * @package @oksai/core
 */

import { Module } from '@nestjs/common';

/**
 * GraphQL API 模块
 *
 * @description
 * 导出所有 GraphQL Resolvers。
 * 当前为空模块，后续可以添加具体的 Resolvers。
 */
@Module({
  imports: [],
  providers: [],
  exports: [],
})
export class GraphqlApiModule {}
