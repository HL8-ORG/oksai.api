# ============================================
# Oksai API 环境变量配置示例
# ============================================
# 复制此文件为 .env 或 .env.development 并根据实际情况修改配置值
# 注意：不要将包含敏感信息的 .env 文件提交到代码库

# ============================================
# 应用基础配置
# ============================================

# Node.js 运行环境 (development | production | test)
NODE_ENV=development

# API 服务器配置
API_HOST=0.0.0.0
API_PORT=3000
API_BASE_URL=http://localhost:3000

# 客户端基础 URL（前端应用地址）
CLIENT_BASE_URL=http://localhost:4200

# ============================================
# 日志配置
# ============================================

# 日志级别 (debug | info | warn | error | verbose)
LOG_LEVEL=debug

# ============================================
# 会话和加密配置
# ============================================

# Express Session 密钥（用于会话加密，生产环境请使用强随机字符串）
EXPRESS_SESSION_SECRET=oksai-session-secret-change-in-production

# 用户密码 Bcrypt 盐轮数（建议 10-12）
USER_PASSWORD_BCRYPT_SALT_ROUNDS=12

# ============================================
# JWT 认证配置
# ============================================

# JWT 访问令牌密钥（生产环境请使用强随机字符串）
JWT_SECRET=secretKey-change-in-production

# JWT 访问令牌过期时间（秒，默认 86400 = 1 天）
JWT_TOKEN_EXPIRATION_TIME=86400

# JWT 刷新令牌密钥（生产环境请使用强随机字符串）
JWT_REFRESH_TOKEN_SECRET=refreshSecretKey-change-in-production

# JWT 刷新令牌过期时间（秒，默认 604800 = 7 天）
JWT_REFRESH_TOKEN_EXPIRATION_TIME=604800

# ============================================
# 数据库配置
# ============================================

# 数据库主机地址
DB_HOST=localhost

# 数据库端口
DB_PORT=5432

# 数据库名称
DB_NAME=oksai

# 数据库用户名
DB_USER=postgres

# 数据库密码
DB_PASSWORD=password

# ORM 类型 (mikro-orm | typeorm)
# 当前仅支持 mikro-orm，typeorm 为预留接口
DB_ORM=mikro-orm

# ============================================
# Redis 配置
# ============================================

# Redis 连接 URL
# 格式: redis://[username]:[password]@[host]:[port]/[database]
REDIS_URL=redis://localhost:6379

# ============================================
# OpenTelemetry 配置（可选）
# ============================================

# 是否启用 OpenTelemetry 追踪
OTEL_ENABLED=false

# OpenTelemetry 提供者 (jaeger | honeycomb | aspecto)
OTEL_PROVIDER=jaeger

# OpenTelemetry 追踪端点
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:14268/api/traces

# OpenTelemetry 服务名称
OTEL_SERVICE_NAME=oksai-api

# ============================================
# Sentry 错误监控配置（可选）
# ============================================

# Sentry DSN（用于错误监控和追踪）
# 获取地址: https://sentry.io/settings/projects/
SENTRY_DSN=

# ============================================
# PostHog 分析配置（可选）
# ============================================

# PostHog API Key
POSTHOG_KEY=

# PostHog 主机地址
POSTHOG_HOST=https://app.posthog.com

# 是否启用 PostHog
POSTHOG_ENABLED=false
